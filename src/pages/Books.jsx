const BooksPage = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        marginTop: "80px",
        color: "#fff",
      }}
    >
      BooksPage
    </div>
  );
};

export default BooksPage;
