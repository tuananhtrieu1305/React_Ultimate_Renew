const ProductPage = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#fff",
      }}
    >
      ProductPage
    </div>
  );
};

export default ProductPage;
