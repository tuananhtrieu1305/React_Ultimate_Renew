const ProductPage = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        marginTop: "80px",
        color: "#fff",
      }}
    >
      ProductPage
    </div>
  );
};

export default ProductPage;
